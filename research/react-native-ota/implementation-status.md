# RN implementation status

Date: 2026-09-12. Branch: `research/react-native-ota-plan`.

RN delivery is partially implemented and remains gated. Production has not been migrated or deployed. Both new migrations have been tested and applied only to the disposable local PostgreSQL database. This document distinguishes working components from release acceptance; a passing server suite does not establish a production-ready updater.

## Implemented

- Shared release service selects behavior from the stored app framework. RN uses platform/runtime/channel locks, PostgreSQL-timed actor-bound preparation intents, durable operation replay, atomic baseline initialization, strictly ordered history, and final-baseline protection. Capacitor retains its existing request-hash bytes, locks, v2 manifest/signature format and native updater.
- RN ZIP/delta initiation and finalization validate full targeting, plaintext content identity, transport identity, inventory and baseline provenance. Ordinary retries require exact agreement and never implicitly adopt a different stored representation. Shared Unicode 15 case-folding/path rules reject aliases, traversal, reserved names and oversized inventories.
- Signed v3 manifests bind app, framework, OS, runtime, channel, version, transport/content hashes, size, encryption, release ID and expiry. Native implementations verify P-256 DER signatures and exact artifact contents.
- Explicit baseline-adoption preparation returns the stored winning representation. The CLI verifier checks the actual archive or delta bytes, inventory, encryption key identity and AES-GCM authentication before recording adoption. It protects an existing incompatible receipt from overwrite. `rn adopt-baseline` now orchestrates ZIP adoption before OTA upload, binding the verified stored representation while preserving the unused local transport. Durable adoption receipts omit download capabilities and tolerate URL rotation.
- `rn prepare-upload` and `rn upload` verify archived build provenance and save both exact ZIP transports before network mutations. Receipts bind server/org/actor/encryption policy. Known sessions resume through an RN-only endpoint without extending their original expiry; lost finalization responses retry the original ID without another PUT. Every retry rechecks archives and transport hashes, encrypted bytes are never regenerated, baseline finalization precedes OTA binding, and publication remains separate. Expired sessions and conflicts require reconciliation rather than silent replacement or adoption.
- Bare RN exporter runs the installed Metro asset mapper, compiles actual Hermes bytecode, composes a private source map, verifies the complete payload and writes immutable export/embedded receipts and ZIP archives. Native-input capture includes installed dependency/autolinking/fingerprint/compiler evidence and rechecks it across export. The transitive native updater-core sources are explicitly included and missing fingerprint evidence is rejected.
- `rn export-embedded`, `rn seal-build` and `rn export` distinguish prepared inputs, a verified completed native package and an OTA export. Sealing verifies the actual APK/iOS app identity, complete embedded payload, receipt and host configuration against the archived baseline, pins the package hash and prevents receipt replacement. OTA export requires that completed receipt and preserves the original baseline privately instead of inventing a new embedded baseline. Native input collection runs outside global Node search paths so package-manager launchers cannot introduce unrelated Expo packages; Hermes is resolved from RN's installed dependency. Automatic native build hooks and complete resolved-input collection remain outstanding; this is local CI package provenance, not store/signature attestation. See the [build workflow](../../packages/cli/src/lib/react-native/README.md).
- `otakit rn prepare-publication` and `otakit rn publish` save an actor/org/server-bound, locked, durable local receipt before sending a mutation. A lost response retries the original operation key and intent; it does not silently prepare a new publication. `rn prepare-collection` now preflights every selected uploaded variant under one display version before writing a collection receipt; `rn publish-collection` validates all replay scopes/windows before sending, preserves independent commits and uncertain operations, and resumes without resending completed targets or preparing replacement intents. Collection upload orchestration remains outstanding.
- RN dashboard rows distinguish platform/runtime variants sharing a version. Publish/revert use prepared intents and persist retry receipts. API/CLI/MCP listing supports optional RN platform/runtime filters; Capacitor preserves its existing list behavior. Baseline deletion conflicts return a typed error before object deletion.
- Expansion migration `20260911120000_rn_delivery_expand` and final constraint migration `20260911180000_rn_target_uniqueness` preserve legacy rows and backfill targeting. The final migration allows RN platform/runtime variants of one version, retains Capacitor version uniqueness with a partial index, and adds RN target checks. The Prisma schema matches the migrated local database.
- Swift core and iOS RN host support signed ZIP/delta installation, AES-GCM, durable verified cache receipts, offline cold-launch verification, generation-scoped readiness/activation, foreground-only timeout, rollback, quarantine and a durable event outbox. Downloads are bounded and reject redirects.
- Java core implements equivalent signature/archive/inventory/cache verification and durable launch recovery. Android host/module integration compiles in Debug and Release. Release emulator acceptance passed on API 24 and API 36, including actual RN headless-task activation deferral. P-256 key validation uses exact curve parameters without requiring an EC AlgorithmParameters provider unavailable on API 24.
- Both native hosts schedule cache collection after durable cold recovery, independently of RN startup and serialized with downloads. Current, last-good, previous confirmed and staged artifacts keep their receipts. Unreferenced artifacts and interrupted staging are removed without following symbolic links; Android also removes owned partial downloads. The optional `previousGood` pointer preserves the prior confirmed artifact without changing rollback selection or invalidating older saved states. Cleanup failures leave launch state intact and retry at a future cold start. This bounds retained code across cold starts, not downloads in an indefinitely running process.
- Metro bootstrap and Expo constants/DOM facades exist. The Expo 57.0.17 / CLI 57.0.19 adapter now consumes the native embed export and validates its full native asset mapping before copying. It preserves a public config snapshot and DOM HTML/JS/assets, keeping native and DOM maps private. Explicit supplemental hashes cover generated native projects and pnpm native dependencies skipped by upstream fingerprint rules; unknown missing evidence still fails. Android archive acceptance passes with an independent Expo/DOM entry. Full Router export is blocked by conflicting icon scale files in Router 57.0.17 and the checked 57.0.21 archive. Expo config-plugin/native host/device acceptance remains incomplete.
- Both native hosts deliver the existing event envelope asynchronously when `ingestURL` is configured. The state transaction records verified download/staging, readiness success, attributed download failure and trial rollback. Retries preserve UUIDs and timestamps; only HTTP 202 acknowledges. Backoff, 256-event/24-hour retention, terminal-error/drop diagnostics, redirect rejection and bounded response handling are implemented. Malformed outbox entries preserve healthy launch pointers. Confirmed-content association changes neither the executing JS context nor old events, and a real embedded baseline switch now retains the outgoing fallback until readiness.
- Native npm archives now contain explicit source inventories and MIT licenses, excluding build output, fixture keys, tests and source maps. The iOS CocoaPods helper resolves the exact installed native-core dependency instead of a repository-relative Podfile path. Isolated pnpm installation, RN autolinking, CocoaPods resolution and both Release native builds passed using actual tarballs outside the workspace. The packages remain private and unpublished.

Capacitor runtime reads still join the bundle to tolerate mixed old/new writers. Switching all Capacitor reads to indexed release snapshots requires an explicit writer-drain cutover. RN creation remains disabled. Do not deploy the final constraint migration while older writers still depend on global version uniqueness.

## Validation completed

- **296 workspace tests passed**, including actual HTTP handler and PostgreSQL integration tests. Real exporter, completed-package and compiled upload integrations are opt-in and excluded from that count. Grouped publication tests cover preflight failures, duplicate lanes, account changes, all-target replay-window validation, overlapping publishers and response loss after independent PostgreSQL commits. Supplemental native-source tests check source edits, generated-resource exclusion, canonical paths and unresolved symlink rejection.
- Local PostgreSQL **16.15**, database `otakit_rn_test`, port **55432**. Migration tests build isolated schemas from legacy migrations, preserve legacy fixtures, apply both new migrations, check old-writer backfill, nullable-runtime uniqueness, baseline foreign keys, RN variants and pending manifest repair, then drop their schemas. Prisma reports **no schema difference** for the migrated local public schema.
- Real Metro **0.84.6**, RN **0.86.3** and Hermes bytecode **98** export integration passed for both iOS and Android. Export/sealing acceptance invokes the built CLI executable, avoiding test-runner resolution shortcuts. Completed-package integration sealed real Release APK/iOS simulator builds and exported OTA variants preserving the exact archived baseline bytes. This does not prove automatic native hooks, signing or complete resolved build evidence.
- Compiled CLI upload acceptance passed against real HTTP handlers and local PostgreSQL using both archived iOS and Android exports: encrypted preparation, baseline-first upload, a deliberately lost finalization response, retry/no-op replay and explicit adoption of the winning baseline ciphertext. Storage uses a local HTTP stand-in; the test checks uploaded hashes and that no API token reaches object requests. It creates no releases.
- **21 Swift tests** passed, covering launch generations/recovery and real signed ZIP/encrypted ZIP/delta fixtures, Unicode paths, corruption and cache verification. Collection tests retain every launch pointer and receipt, avoid external symlink targets, preserve state/outbox bytes, and permit reinstall after corrupt unreferenced cache removal. Event tests cover lost responses/restarts, concurrent flushes, bounded retries, terminal diagnostics, queue expiry, legacy timestamps, malformed entries, failed durable acknowledgements, association and embedded-trial recovery. Java core tests cover the matching retry, retention, attribution and embedded-switch contracts.
- Java core tests passed for recovery, readiness, quarantine, foreground timeout, stale generations, signatures, real ZIP/AES-GCM inventories, offline expired-cache acceptance, corrupted-cache fallback, incomplete saved trial identity and terminal storage-write failure. Additional checks cover durable previous-good retention, partial download/staging cleanup, linked namespaces, external symlink targets and reinstall after corrupt cache collection.
- iOS **Debug and Release** builds compiled against installed RN 0.86.3. On an iPhone 17 Pro/iOS 26.5 simulator, the latest Release passed nine scenarios: encrypted OTA activation/readiness, delta activation, foreground timeout rollback/quarantine, cold cache verification, asynchronous cache collection with direct filesystem inspection, tampered signature rejection, oversized chunked download rejection, redirect rejection, and process-death recovery with one durable rollback. Previous Debug acceptance passed seven scenarios; the new collection check ran in Release.
- Android **API 24 and API 36 Release** emulators passed those nine scenarios plus a real RN headless task that defers activation, retains the staged update and permits activation in a fresh foreground process. Cache inspection used rooted local emulator shells and confirmed that only current/previous-good artifacts and their receipts survived cleanup; adbd was restored to its original non-root state afterward.
- The isolated tarball installation passed nine Release scenarios on iOS and nine on Android API 36 (the Android packed run omitted optional root-only cache-file inspection). This validates package installation and native execution for RN 0.86.3 without repository links; RN 0.87 and Expo package acceptance remain outstanding.
- After event delivery and baseline-association changes, the workspace Release builds passed **11 device scenarios each on iOS 26.5, Android API 24 and Android API 36**. The new checks prove an embedded baseline switch with readiness but no ZIP request, association without another success or rewriting the JS context, and event delivery after simulated 503s and a lost acceptance response. The local receiver verifies stable retry IDs/bodies, release attribution and deduplication; the outbox is empty after cold restart. iOS includes direct cache-file inspection; these two Android runs used non-root emulators and included headless-task deferral. Fresh completed-package checks also sealed both final Release packages and exported OTA artifacts preserving their exact baselines.
- Workspace type checks, changed-file lint and CLI build passed.
- The opt-in compiled Expo exporter test passed on Android with SDK 57.0.17 / CLI 57.0.19: actual Hermes bytecode, config snapshot, DOM HTML/JS, private maps, complete ZIP verification, supplemental native dependency evidence, and rejection of the Router icon collision before any published export directory exists. This is prepared-export acceptance, not an Expo native build or device run.

The server tests mock external storage/CDN, authentication and audit delivery while exercising real local PostgreSQL. Device tests use generated test keys and localhost CDN/ingest stand-ins. They do not establish physical-device, live storage/CDN/telemetry or Expo acceptance. Fixture resource preparation is not a production native build hook; post-build sealing verifies its actual packaged output. The separate tarball fixture proves installed native package execution, not automatic native build evidence or store signing.

## Reproduce local checks

Use a disposable local database only:

```sh
export DATABASE_URL='postgresql://otakit_test@127.0.0.1:55432/otakit_rn_test?schema=public'
pnpm --filter @otakit/console db:generate
pnpm --filter @otakit/console db:migrate
RUN_DATABASE_TESTS=1 pnpm test
pnpm typecheck
pnpm --filter @otakit/cli build
RUN_RN_EXPORT_TESTS=1 pnpm --filter @otakit/cli test -- src/lib/react-native/export.integration.test.ts
RUN_DATABASE_TESTS=1 RUN_RN_UPLOAD_CLI_TESTS=1 RN_OTA_EXPORT="$OTAKIT_OTA_DIR" \
  pnpm --filter @otakit/console test -- lib/services/rn-uploads.integration.test.ts
```

See `examples/react-native-app/README.md` for native fixture requirements and execution.

## Outstanding release work

1. Finish Expo config-plugin/native integration and expand exporter acceptance to iOS. Resolve the upstream Router icon collision, then exercise Router/splash/DOM behavior with real Expo builds. The independent Android Expo/DOM export is verified; native Expo acceptance remains separate from bare RN.
2. Automate native build hooks and complete resolved native-input evidence around the implemented package sealing and OTA baseline receipts. Add AAB/IPA/signing acceptance. Complete delta/collection upload orchestration; single-variant ZIP upload, explicit adoption and durable grouped publication are implemented.
3. Complete default update policy and exception/lifecycle acceptance. Native event delivery and cold-start cache collection are implemented; extend device fault injection for protected/unavailable storage, interrupted writes/downloads and cleanup errors, alongside process death, stale callbacks and headless launches.
4. Run dashboard browser acceptance and grouped variant reconciliation. Extend installed-package acceptance to the supported RN/Expo version matrix, physical iOS/Android devices and live delivery. RN 0.86.3 tarball installation and Android API 24/36 emulator coverage are established; they do not cover the full device matrix.
5. Review deployment sequencing and writer drain before any production migration or RN enablement. Production migration remains explicitly unauthorized.
