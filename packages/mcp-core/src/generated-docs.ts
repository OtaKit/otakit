// Generated by scripts/generate-llms-txt.mjs. Do not edit manually.
import type { DocumentationPage } from './docs';

export const GENERATED_DOCUMENTATION = [
  {
    title: 'Docs',
    label: 'Overview',
    path: '/docs',
    description: 'How OtaKit works and where to start.',
    markdown:
      "OtaKit ships over-the-air updates for Capacitor apps. You build your web app, upload its bundle, and the plugin delivers that bundle to devices without store submissions or reviews.\n\nStart with the quick setup\n\n### How it works\n1. Create an app in the OtaKit dashboard and copy its `appId` into your `capacitor.config.ts` file.\n2. Call `notifyAppReady()` when your app finishes loading, so newly activated updates can be confirmed healthy.\n3. Build your web app and run `otakit upload --release` to upload and publish a new bundle.\n4. On device, the plugin checks for updates, verifies the downloaded bundle, and by default activates it on the next cold launch.\n\n### Features\n- **One-command shipping**: Build your web app, then release with otakit upload --release.\n- **Channels & runtime lanes**: Channels for rollout tracks, runtimeVersion for native compatibility lanes — switchable at runtime with setChannel().\n- **Delta updates**: Opt-in per-file updates: devices download only the files that changed between releases.\n- **Automatic update delivery**: The normal flow checks and stages automatically, then activates according to the configured launch, resume, and runtime policies.\n- **Force-immediate releases**: Emergency releases apply and reload on the device's next check, whatever its configured policies.\n- **Update lifecycle events**: updateAvailable, updateStaged, updateApplied, downloadFailed, rollback — build your own update UX.\n- **Manual update control**: Turn automatic policies off when your app wants to show its own update prompt or control install timing.\n- **Safe activation & rollback**: A newly activated bundle must call notifyAppReady() or OtaKit rolls back automatically.\n- **Signed & verified**: ES256-signed manifests and SHA-256 checks on every download, plus optional end-to-end encryption.\n- **Native compatibility guardrail**: The CLI warns at upload time when a bundle depends on native code the installed app doesn't have.\n- **AgentKit: MCP & Agent Skills**: Let coding agents inspect projects, package and upload builds, prepare releases, monitor events, and revert through the same OtaKit workflow.\n- **Self-hosting**: Run OtaKit on your own infrastructure when you need full control over delivery and trust.\n\n### Getting started\n- **Quick setup**: Connect the default hosted OtaKit flow to your Capacitor app.\n- **Next.js Guide**: Go from Next.js + Capacitor to your first OTA update.\n- **Channels & Runtimes**: Rollout tracks vs runtime compatibility lanes, and when to use each.\n- **Update Strategies**: How launchPolicy, resumePolicy, and runtimePolicy combine to control when updates apply.\n- **Events & Listeners**: Subscribe to update lifecycle events: available, staged, applied, failed, rolled back.\n- **AgentKit**: Connect MCP and the OtaKit Agent Skill to Codex, Claude Code, VS Code, and other agents.\n- **CI Automation**: Build and ship bundles from GitHub Actions.\n- **CLI Reference**: Commands, options, and release workflows.\n- **Plugin API**: Default automatic flow, manual flow, and configuration.\n\nNeed help with setup, billing, or rollout issues? Email support@otakit.app .",
  },
  {
    title: 'Quick setup',
    label: 'Setup',
    path: '/docs/setup',
    description: 'Set up the default hosted OtaKit flow in a Capacitor project.',
    markdown:
      'This is the most simple, default hosted setup path.\n\n### 1. Create your app in the dashboard\n\nSign in to the OtaKit dashboard , create an app, and copy its OtaKit `appId`.\n\n### 2. Install the Capacitor plugin\n\n```txt\nnpm install @otakit/capacitor-updater\nnpx cap sync\n```\n\n### 3. Configure the plugin\n\nAdd the OtaKit plugin to `capacitor.config.ts` and paste in the `appId` from the dashboard.\n\n```txt\n// capacitor.config.ts\nimport type { CapacitorConfig } from "@capacitor/cli";\n\nconst config: CapacitorConfig = {\n  appId: "com.example.myapp",\n  appName: "My App",\n  webDir: "out",\n  plugins: {\n    OtaKit: {\n      appId: "YOUR_OTAKIT_APP_ID",\n    }\n  }\n};\n\nexport default config;\n```\n\nNote: Your app must be published to the app store at least once with the OtaKit plugin configured before it can receive updates!\n\nOptional: if you want something other than the hosted defaults, you can also set `launchPolicy`, `resumePolicy`, `runtimePolicy`, and `checkInterval`. See the Plugin API page for the full configuration surface.\n\n### 4. Add notifyAppReady()\n\nCall `notifyAppReady()` once your app has loaded. If the new bundle is activated and your app never confirms that it started successfully, OtaKit rolls back automatically.\n\n```txt\nimport { OtaKit } from "@otakit/capacitor-updater";\n\nawait OtaKit.notifyAppReady();\n```\n\nFor React-style apps, wrap it in a client-side effect:\n\n```txt\n"use client";\nimport { useEffect } from "react";\nimport { Capacitor } from "@capacitor/core";\nimport { OtaKit } from "@otakit/capacitor-updater";\n\nexport function AppReadyProvider() {\n  useEffect(() => {\n    if (Capacitor.isNativePlatform()) {\n      OtaKit.notifyAppReady();\n    }\n  }, []);\n\n  return null;\n}\n```\n\n### 5. Install the CLI and sign in\n\n```txt\nnpm install -g @otakit/cli\notakit login\n```\n\n### 6. Build and release\n\n```txt\nnpm run build\notakit upload --release\n```\n\nThat publishes the bundle to the base channel. By default, OtaKit downloads it in the background and activates it on the next cold app launch.\n\n### Next\n- Continue with the Next.js guide or React guide\n- Read Channels & Runtimes for more advanced release flows.\n- Use the Plugin API and CLI reference for exact configuration and command details.',
  },
  {
    title: 'CLI Reference',
    label: 'CLI Reference',
    path: '/docs/cli',
    description:
      'Upload bundles, release them, inspect history, and manage apps with the OtaKit CLI.',
    markdown:
      'Use the CLI to upload bundles, release them, inspect bundle and release history, and manage apps.\n\n### Project config\n\nProject commands read from `capacitor.config.*`.\n\n```txt\n// capacitor.config.ts\nimport type { CapacitorConfig } from "@capacitor/cli";\n\nconst config: CapacitorConfig = {\n  appId: "com.example.myapp",\n  appName: "My App",\n  webDir: "out",\n  plugins: {\n    OtaKit: {\n      appId: "app_xxxxxxxx",\n      // Optional named channel:\n      // channel: "staging"\n      // Optional compatibility lane:\n      // runtimeVersion: "2026.04"\n    }\n  }\n};\n\nexport default config;\n```\n\n### Authentication\n\nFor local development, sign in once and the CLI stores a token locally. For CI or non-interactive environments, use an organization secret key instead.\n\n```txt\n# Local development\notakit login\n\n# CI / non-interactive\nexport OTAKIT_TOKEN=otakit_sk_...\nexport OTAKIT_APP_ID=app_xxxxxxxx\n```\n\n### Release flow\n- Upload only: `otakit upload`\n- Upload and release to the base channel: `otakit upload --release`\n- Upload and release to a named channel: `otakit upload --release beta`\n- Promote an existing bundle later: `otakit release <bundleId> --channel production`\n\n### Resolution order\n\nThe CLI resolves values in a deterministic order.\n- App ID: `--app-id` -> `OTAKIT_APP_ID` -> `capacitor.config.*`\n- Server URL: `--server` -> `OTAKIT_SERVER_URL` -> `plugins.OtaKit.serverUrl` -> hosted default\n- Auth token: `OTAKIT_TOKEN` -> stored login token\n- Upload path: CLI path argument -> `OTAKIT_BUILD_DIR` -> `capacitor.config.* webDir`\n- Release channel: `--release` -> base channel, `--release <channel>` -> named channel\n- Runtime version: `plugins.OtaKit.runtimeVersion` -> bundle metadata during upload\n- Upload version: `--version` -> `OTAKIT_VERSION` -> auto-generated version\n\n### Command reference\n\n### otakit upload [path]\n\nUpload a bundle. Optionally release it immediately.\n\nOptions\n- `[path]`: Bundle directory. If omitted, the CLI uses OTAKIT_BUILD_DIR or capacitor.config.* webDir.\n- `--app-id <id>`: App ID override.\n- `--server <url>`: Server URL override.\n- `--version <version>`: Version string. Otherwise OTAKIT_VERSION, then auto-generated.\n- `--strict-version`: Require explicit or env-provided version.\n- `--release [channel]`: Release after upload. Omit channel to release to the base channel.\n- `--strategy <strategy>`: Upload strategy: "zip" (single archive, default) or "deltas" (per-file objects; devices download only what changed).\n- `--force-immediate`: With --release: devices apply and reload this release on their next check (emergency fixes).\n- `--encrypt`: Encrypt the bundle with OTAKIT_ENCRYPTION_KEY before upload (auto-enabled when the env var is set).\n- `--fail-on-incompatible`: Exit non-zero when the native compatibility check finds changes that need a store build.\n- `--ignore-compat`: Skip the native compatibility check.\n\nExample\n\n```txt\notakit upload --release\n```\n\n### otakit release [bundleId]\n\nRelease a bundle to the base channel or a named channel. The bundle already carries its runtimeVersion, so release only chooses the rollout channel.\n\nOptions\n- `--channel <channel>`: Target named channel. Omit it to use the base channel.\n- `--force-immediate`: Devices apply and reload this release on their next check (emergency fixes).\n\nExample\n\n```txt\notakit release --channel production\n```\n\n### otakit compatibility\n\nCheck the local native plugin set against a channel\'s current release without uploading.\n\nOptions\n- `--channel <channel>`: Channel to compare against. Omit for the base channel.\n- `--package-json <path>`: package.json used for native dependency detection.\n- `--node-modules <path>`: node_modules used for native dependency detection.\n\nExample\n\n```txt\notakit compatibility --channel production\n```\n\n### otakit list\n\nList uploaded bundles.\n\nOptions\n- `--limit <n>`: Max results. Defaults to 20.\n\nExample\n\n```txt\notakit list --limit 20\n```\n\n### otakit releases\n\nShow release history across all streams or a specific target.\n\nOptions\n- `--channel <channel>`: Show only a named channel.\n- `--base`: Show only the base channel.\n- `--limit <n>`: Max results. Defaults to 10.\n\nExample\n\n```txt\notakit releases --base\n```\n\n### otakit delete <bundleId>\n\nDelete a bundle.\n\nOptions\n- `--force`: Skip confirmation prompt.\n\nExample\n\n```txt\notakit delete abc123 --force\n```\n\n### otakit register\n\nCreate a new app and print the plugin snippet to paste into capacitor.config.ts.\n\nOptions\n- `--slug <slug>`: App slug (for example com.example.app).\n- `--server <url>`: Server URL override.\n- `--token <token>`: Access token or organization API key.\n- `--secret-key <key>`: Alias for --token.\n\nExample\n\n```txt\notakit register --slug com.example.myapp\n```\n\n### otakit login\n\nSign in with email OTP and store a token locally.\n\nOptions\n- `--email <email>`: Email address. If omitted, prompts interactively.\n- `--server <url>`: Server URL override.\n- `--token-only`: Print token to stdout only.\n\nExample\n\n```txt\notakit login --email you@example.com\n```\n\n### otakit whoami\n\nShow current authenticated user and organization context.\n\nOptions\n- `--server <url>`: Server URL override.\n\nExample\n\n```txt\notakit whoami\n```\n\n### otakit logout\n\nRemove stored token for a server.\n\nOptions\n- `--server <url>`: Server URL override.\n\nExample\n\n```txt\notakit logout\n```\n\n### otakit config resolve\n\nShow effective CLI values and where they came from.\n\nOptions\n- `--app-id <id>`: App ID override.\n- `--server <url>`: Server URL override.\n- `--output-dir <path>`: Output directory override.\n- `--channel <channel>`: Channel override.\n- `--json`: Print machine-readable JSON output.\n\nExample\n\n```txt\notakit config resolve --json\n```\n\n### otakit mcp\n\nStart the AgentKit local MCP server, bound to one project and organization for its lifetime.\n\nOptions\n- `--project-root <path>`: Project root available to local MCP tools.\n- `--organization-id <id>`: Bind the server to one organization for its lifetime.\n\nExample\n\n```txt\nnpx -y @otakit/cli@1.5.0 mcp --project-root .\n```\n\n### otakit config validate\n\nValidate the OtaKit-related values in capacitor.config.*.\n\nOptions\n- `--json`: Print machine-readable JSON output.\n\nExample\n\n```txt\notakit config validate\n```\n\n### otakit generate-signing-key\n\nGenerate an ES256 key pair for manifest signing.\n\nExample\n\n```txt\notakit generate-signing-key\n```\n\n### otakit generate-encryption-key\n\nGenerate an AES-256 bundle encryption key. Keep it in CI as OTAKIT_ENCRYPTION_KEY and ship it in the app\'s bundleKeys config.\n\nExample\n\n```txt\notakit generate-encryption-key\n```\n\n### Troubleshooting\n- Missing app ID: add `plugins.OtaKit.appId` to `capacitor.config.ts`, or pass `--app-id`.\n- Missing `index.html`: build your web app and verify `webDir` or the explicit upload path.\n- Need to create an app from automation: use `otakit register --slug <slug>`.',
  },
  {
    title: 'Plugin API',
    label: 'Plugin API',
    path: '/docs/plugin',
    description:
      'Capacitor plugin setup, policy-based automatic updates, and manual advanced flows.',
    markdown:
      'Import from `@otakit/capacitor-updater`. The default flow usually only needs `notifyAppReady()`. The other public methods exist for advanced manual update flows where your app decides when to check, download, and apply an update.\n\n```txt\nimport { OtaKit } from "@otakit/capacitor-updater";\n```\n\n### Configuration\n\nHosted OtaKit keeps the config small. The default automatic behavior is runtime catch-up on cold start, staged activation on later cold starts, and background checks on resume.\n\n```txt\nplugins: {\n  OtaKit: {\n    appId: "YOUR_OTAKIT_APP_ID",\n    // Optional:\n    // channel: "production",\n    // runtimeVersion: "2026.04",\n    // launchPolicy: "apply-staged",\n    // resumePolicy: "shadow",\n    // runtimePolicy: "immediate",\n    // checkInterval: 600000,\n  }\n}\n```\n- `appId` (string): OtaKit app ID for manifest fetches and event ingest.\n- `channel` (string): Named release track to check. Omit it to use the base channel.\n- `runtimeVersion` (string): Optional native compatibility lane. Set it when a new store build must stop receiving older OTA bundles.\n- `launchPolicy` ("off" | "shadow" | "apply-staged" | "immediate"): Cold-start policy after the current runtime lane is already resolved. Default: apply-staged.\n- `resumePolicy` ("off" | "shadow" | "apply-staged" | "immediate"): Foreground resume policy. Default: shadow.\n- `runtimePolicy` ("off" | "shadow" | "apply-staged" | "immediate"): Cold-start policy used when runtimeVersion changes or resolves for the first time — in practice, the app\'s first native install, or a native update where you bumped runtimeVersion in the plugin config. Default: immediate.\n- `checkInterval` (number): Milliseconds between automatic background resume checks. Applies to `resumePolicy: "shadow"` and `resumePolicy: "apply-staged"` when no staged bundle is already waiting. Default: 600000. Set to 0 or a negative value to disable resume throttling.\n- `appReadyTimeout` (number): Milliseconds to wait for notifyAppReady(). Default: 10000.\n- `cdnUrl` (string): Optional CDN base URL for static manifest and bundle delivery. Leave unset for the hosted default.\n- `ingestUrl` (string): Optional event ingest base URL. Leave unset for the hosted default.\n- `serverUrl` (string): Optional control-plane API base URL used by self-host tooling such as the CLI. The native runtime uses cdnUrl and ingestUrl instead.\n- `manifestKeys` (array): Optional public verification keys for custom or self-hosted manifest signing.\n- `allowInsecureUrls` (boolean): Allow HTTP only for localhost development. Default: false.\n\nHosted OtaKit points at the managed CDN and ingest service automatically. Do not set `cdnUrl`, `ingestUrl`, or `manifestKeys` unless you intentionally want custom hosting or verification behavior.\n\n### Policies\n\nPolicies are the only automatic behaviors. The same policy logic runs no matter which event triggered it. `launchPolicy`, `resumePolicy`, and `runtimePolicy` only decide when to invoke that policy.\n- `off` (policy): Do nothing automatically.\n- `shadow` (policy): Check for the latest update and stage it locally, but never apply it in that flow.\n- `apply-staged` (policy): Apply an already staged bundle if one exists. If nothing is staged, fall back to shadow.\n- `immediate` (policy): Check, stage, and immediately apply the newest update when one is available.\n\nThe hosted defaults are usually the right production baseline: `runtimePolicy: "immediate"`, `launchPolicy: "apply-staged"`, and `resumePolicy: "shadow"`.\n\n### Compatibility lanes\n\n`channel` is for rollout audience. `runtimeVersion` is for native compatibility.\n\nIf you ship a new store build and do not want it to keep consuming older OTA bundles, bump `runtimeVersion` in the plugin config before uploading the next OTA bundle.\n\nThe CLI reads that same value automatically during upload, so releases stay inside the correct runtime lane without extra flags.\n\nWhen `runtimeVersion` changes, or when the app resolves a runtime lane for the first time, OtaKit runs `runtimePolicy` on cold start. In practice that means the app\'s first native install, or a native update where you bumped `runtimeVersion` in the plugin config. By default that policy is `immediate`, so fresh installs and new native shells catch up before that lane is marked resolved.\n\nWant the full breakdown of how `launchPolicy`, `resumePolicy`, and `runtimePolicy` combine? See the Update Strategies guide.\n\n### Automatic Flow (Default)\n\nThe hosted default is equivalent to this configuration:\n\n```txt\nplugins: {\n  OtaKit: {\n    appId: "YOUR_OTAKIT_APP_ID",\n    runtimePolicy: "immediate",\n    launchPolicy: "apply-staged",\n    resumePolicy: "shadow",\n    appReadyTimeout: 10000,\n  }\n}\n```\n\nIn this mode, your app code usually only needs to call `notifyAppReady()`.\n\nResume throttling is intentionally narrow: `checkInterval` only affects resume background checks. Cold-start runtime handling, cold-start launch handling, and all manual APIs always act immediately.\n\nSet `checkInterval` to `0` or a negative value if you want resume checks to run every time instead of being throttled.\n- `notifyAppReady()` -> `void`: Confirm the current bundle is working. Call this once when your app has fully loaded. If it is not called within appReadyTimeout, the plugin rolls back.\n\n```txt\nimport { OtaKit } from "@otakit/capacitor-updater";\n\nawait OtaKit.notifyAppReady();\n```\n\n### Manual Flow (Advanced)\n\nIf you want no automatic checks at all, turn every automatic policy off and drive the update lifecycle yourself.\n\n```txt\nplugins: {\n  OtaKit: {\n    appId: "YOUR_OTAKIT_APP_ID",\n    launchPolicy: "off",\n    resumePolicy: "off",\n    runtimePolicy: "off",\n  }\n}\n```\n- `getState()` -> `OtaKitState`: Inspect the current updater state: current bundle, fallback bundle, staged bundle, and builtin version.\n- `check()` -> `CheckResult`: Check the configured channel for a newer bundle without downloading it. Returns no_update, already_staged, or update_available.\n- `download()` -> `DownloadResult`: Ensure the latest bundle is staged for later activation. Returns no_update or staged.\n- `update()` -> `void`: Convenience helper for manual flows. It runs the same native immediate-flow operation used by automatic immediate policies. Terminal operation: if an update is applied, it does not resolve back into the old JS context.\n- `apply()` -> `void`: Activate the currently staged bundle and reload the WebView. Terminal operation: on success it does not resolve back into the old JS context.\n- `notifyAppReady()` -> `void`: Still required after the updated bundle launches. Call this once when your app has fully loaded so the plugin can mark the new bundle healthy.\n- `getLastFailure()` -> `BundleInfo | null`: Returns information about the most recent failed update (rollback). Useful for diagnostics and crash reporting. Returns null if no failure has occurred.\n\nThe plugin also emits lifecycle events — `updateAvailable`, `updateStaged`, `updateApplied`, `downloadFailed`, and `rollback` — via `OtaKit.addListener(...)`. See Events & Listeners for the full reference and the startup reconciliation pattern.\n\nAfter a successful `apply()` or an `update()` that installs a new bundle, the app reloads immediately. Call `notifyAppReady()` from the reloaded app startup, not in the same JS flow that triggered the activation.\n\nThe simplest manual pattern is: check for updates, show your own prompt, then call `update()` if the user accepts.\n\n```txt\nconst latest = await OtaKit.check();\n\nif (latest.kind === "no_update") {\n  return;\n}\n\nconst accepted = window.confirm("Update available. Install now?");\n\nif (accepted) {\n  await OtaKit.update();\n}\n```\n\nIf you want a split flow, download first and apply later:\n\n```txt\nconst state = await OtaKit.getState();\n\nif (state.staged) {\n  await OtaKit.apply();\n  return;\n}\n\nconst latest = await OtaKit.check();\nif (latest.kind === "no_update") {\n  return;\n}\n\nconst accepted = window.confirm("Update available. Download now?");\nif (!accepted) {\n  return;\n}\n\nconst result = await OtaKit.download();\nif (result.kind === "no_update") {\n  return;\n}\n\n// Later, after another user action:\nawait OtaKit.apply();\n```\n\n### Advanced Overrides\n\nUse these only when you run a custom server or need custom verification behavior.\n\n```txt\nplugins: {\n  OtaKit: {\n    appId: "YOUR_OTAKIT_APP_ID",\n    // Optional advanced overrides\n    // cdnUrl: "https://cdn.your-domain.com",\n    // ingestUrl: "https://ingest.your-domain.com/v1",\n    // serverUrl: "https://your-domain.com/api/v1",\n    // allowInsecureUrls: false,\n    // manifestKeys: [\n    //   { kid: "key-2026-01", key: "MFkwEwYH..." }\n    // ]\n  }\n}\n```\n- `cdnUrl` (string): Custom CDN base URL for static manifest and bundle delivery.\n- `ingestUrl` (string): Custom event ingest base URL used for device telemetry.\n- `serverUrl` (string): Control-plane API URL used by self-host tooling such as the CLI. The native runtime does not read it.\n- `allowInsecureUrls` (boolean): Allow HTTP for localhost development only. Default: false.\n- `manifestKeys` (array): Public verification keys for manifest signature verification on custom/self-hosted setups.\n\n### Types\n\n```txt\ntype OtaKitPolicy = "off" | "shadow" | "apply-staged" | "immediate";\n\ninterface BundleInfo {\n  id: string;\n  version: string;\n  runtimeVersion?: string;\n  status: "builtin" | "pending" | "trial" | "success" | "error";\n  downloadedAt?: string;\n  sha256?: string;\n  channel?: string;\n  releaseId?: string;\n}\n\ninterface OtaKitState {\n  current: BundleInfo;\n  fallback: BundleInfo;\n  staged: BundleInfo | null;\n  builtinVersion: string;\n}\n\ninterface LatestVersion {\n  version: string;\n  runtimeVersion?: string;\n  url: string;\n  sha256: string;\n  size: number;\n  releaseId: string;\n}\n\ntype CheckResult =\n  | { kind: "no_update" }\n  | { kind: "already_staged"; latest: LatestVersion }\n  | { kind: "update_available"; latest: LatestVersion };\n\ntype DownloadResult =\n  | { kind: "no_update" }\n  | { kind: "staged"; bundle: BundleInfo };\n```',
  },
  {
    title: 'REST API',
    label: 'REST API',
    path: '/docs/api',
    description: 'Public OtaKit REST API endpoints for app, bundle, and release automation.',
    markdown:
      'All endpoints are under `/api/v1`. This page covers the public Bearer-token API for automation and server-side tooling. Requests and responses use JSON.\n\n### Authentication\n\nPublic REST requests use a Bearer token:\n\nBearer token CLI / server operations — upload, release, list. Use your organization secret key ( `otakit_sk_...`) or a user access token from `otakit login`. The app ID is part of the URL path.\n\n```txt\n# CLI / server operations\nAuthorization: Bearer otakit_sk_...\n```\n\n### Apps\n\n### POST /api/v1/apps\n\nCreate a new app. The slug should match your Capacitor app identifier. Returns an appId for plugin and CLI usage.\n- Auth: Bearer\n\nRequest body\n\n```txt\n{ "slug": "com.example.myapp" }\n```\n\nResponse\n\n```txt\n{\n  "id": "uuid",\n  "slug": "com.example.myapp",\n  "createdAt": "ISO timestamp"\n}\n```\n\n### Bundles\n\n### POST /api/v1/apps/:appId/bundles/initiate\n\nStart a bundle upload session. Returns a presigned PUT URL. Upload your zip file to this URL with Content-Type: application/zip.\n- Auth: Bearer\n\nRequest body\n\n```txt\n{\n  "version": "1.0.1",       // semver string\n  "size": 1048576,          // bundle size in bytes\n  "sha256": "64-char hex checksum of the zip file",\n  "runtimeVersion": "2026.04" // optional compatibility lane\n}\n```\n\nResponse\n\n```txt\n{\n  "uploadId": "uuid",\n  "presignedUrl": "https://...",\n  "storageKey": "...",\n  "expiresAt": "ISO timestamp"\n}\n```\n\n### POST /api/v1/apps/:appId/bundles/finalize\n\nFinalize a bundle upload session. The server checks that the uploaded object exists and that its size matches the initiated session, then creates the bundle record from the stored session data.\n- Auth: Bearer\n\nRequest body\n\n```txt\n{\n  "uploadId": "uuid"\n}\n```\n\nResponse\n\n```txt\n{\n  "id": "uuid",\n  "version": "1.0.1",\n  "sha256": "...",\n  "size": 1048576,\n  "runtimeVersion": "2026.04",\n  "createdAt": "ISO timestamp"\n}\n```\n\n### GET /api/v1/apps/:appId/bundles\n\nList bundles sorted by creation date (newest first).\n- Auth: Bearer\n- Query: ?limit=20&offset=0\n\nResponse\n\n```txt\n{\n  "bundles": [{ id, version, sha256, size, createdAt }],\n  "total": 42\n}\n```\n\n### DELETE /api/v1/apps/:appId/bundles/:bundleId\n\nDelete a bundle. Bundles that are part of a release history cannot be deleted.\n- Auth: Bearer\n\nResponse\n\n```txt\n{ "deleted": true, "id": "uuid" }\n```\n\n### Releases\n\n### POST /api/v1/apps/:appId/releases\n\nRelease a bundle to the base channel or a named channel. The runtimeVersion comes from the bundle itself, so current resolution is per (channel, runtimeVersion).\n- Auth: Bearer\n\nRequest body\n\n```txt\n{\n  "bundleId": "uuid",\n  "channel": "staging"   // optional; omit or null for base channel\n}\n```\n\nResponse\n\n```txt\n{\n  "release": {\n    "id": "uuid",\n    "channel": null,\n    "runtimeVersion": "2026.04",\n    "bundleId": "uuid",\n    "bundleVersion": "1.0.1",\n    "promotedAt": "ISO timestamp"\n  },\n  "previousRelease": { ... } | null\n}\n```\n\n### GET /api/v1/apps/:appId/releases\n\nList release history sorted newest first. Omit channel to list every stream, or pass an empty channel value to query only the base channel.\n- Auth: Bearer\n- Query: ?channel=staging&limit=20&offset=0\n\nResponse\n\n```txt\n{\n  "releases": [{\n    id, channel, runtimeVersion, bundleId, bundleVersion, promotedAt\n  }],\n  "total": 12\n}\n```',
  },
  {
    title: 'Next.js Guide',
    label: 'Next.js Guide',
    path: '/docs/guide',
    description: 'Step-by-step guide: from Next.js + Capacitor to your first OTA update.',
    markdown:
      'This walkthrough takes a Next.js app from zero to its first OTA update with the default hosted OtaKit flow.\n\n### 1. Create the Next.js app\n\n```txt\nnpx create-next-app@latest my-app\ncd my-app\n```\n\n### 2. Configure Next.js for static export\n\n```txt\n// next.config.ts\nconst nextConfig = {\n  output: "export",\n};\n\nexport default nextConfig;\n```\n\n### 3. Add Capacitor\n\n```txt\nnpm install @capacitor/core @capacitor/cli\nnpx cap init my-app com.example.myapp\n```\n\nSet `webDir` to `out` in `capacitor.config.ts`:\n\n```txt\nimport type { CapacitorConfig } from "@capacitor/cli";\n\nconst config: CapacitorConfig = {\n  appId: "com.example.myapp",\n  appName: "my-app",\n  webDir: "out",\n};\n\nexport default config;\n```\n\n### 4. Add native platforms\n\n```txt\nnpm install @capacitor/ios @capacitor/android\nnpx cap add ios\nnpx cap add android\n```\n\n### 5. Install the OtaKit plugin\n\n```txt\nnpm install @otakit/capacitor-updater\nnpx cap sync\n```\n\n### 6. Create an OtaKit app and log in\n\nCreate an app in the OtaKit dashboard and copy its `appId`. Then install the CLI and log in:\n\n```txt\nnpm install -g @otakit/cli\notakit login\n```\n\n### 7. Configure the plugin\n\n```txt\nconst config: CapacitorConfig = {\n  appId: "com.example.myapp",\n  appName: "my-app",\n  webDir: "out",\n  plugins: {\n    OtaKit: {\n      appId: "YOUR_OTAKIT_APP_ID",\n    }\n  }\n};\n```\n\n### 8. Add notifyAppReady()\n\nCreate a client component that confirms the app loaded successfully:\n\n```txt\n// app/components/AppReady.tsx\n"use client";\nimport { useEffect } from "react";\nimport { Capacitor } from "@capacitor/core";\nimport { OtaKit } from "@otakit/capacitor-updater";\n\nexport function AppReady() {\n  useEffect(() => {\n    if (Capacitor.isNativePlatform()) {\n      OtaKit.notifyAppReady();\n    }\n  }, []);\n\n  return null;\n}\n```\n\nAdd it to your root layout:\n\n```txt\n// app/layout.tsx\nimport { AppReady } from "./components/AppReady";\n\nexport default function RootLayout({ children }) {\n  return (\n    <html lang="en">\n      <body>\n        <AppReady />\n        {children}\n      </body>\n    </html>\n  );\n}\n```\n\n### 9. Build and run on a device\n\n```txt\nnpm run build\nnpx cap sync\nnpx cap run ios       # or: npx cap run android\n```\n\nNote: The app must be published to the App Store (and/or Play Store) at least once with the OtaKit plugin configured before end users&rsquo; devices can receive live updates.\n\n### 10. Ship your first OTA update\n\nMake a visible change to your app, rebuild, and release:\n\n```txt\nnpm run build\notakit upload --release\n```\n\nRelaunch the app on your device. By default, OtaKit downloads the update in the background and activates it on the next cold launch.\n\n### Next\n- Read Channels & Runtimes for release tracks and native compatibility boundaries.\n- Use the Plugin API and CLI reference for exact configuration and command details.',
  },
  {
    title: 'React Guide',
    label: 'React Guide',
    path: '/docs/react',
    description: 'Step-by-step guide: from Vite + React + Capacitor to your first OTA update.',
    markdown:
      'This walkthrough uses Vite + React with Capacitor and the default hosted OtaKit flow.\n\n### 1. Create the React app\n\n```txt\nnpm create vite@latest my-app -- --template react-ts\ncd my-app\nnpm install\n```\n\n### 2. Add Capacitor\n\n```txt\nnpm install @capacitor/core @capacitor/cli\nnpx cap init my-app com.example.myapp\n```\n\nSet `webDir` to `dist` in `capacitor.config.ts`:\n\n```txt\nimport type { CapacitorConfig } from "@capacitor/cli";\n\nconst config: CapacitorConfig = {\n  appId: "com.example.myapp",\n  appName: "my-app",\n  webDir: "dist",\n};\n\nexport default config;\n```\n\n### 3. Add native platforms\n\n```txt\nnpm install @capacitor/ios @capacitor/android\nnpx cap add ios\nnpx cap add android\n```\n\n### 4. Install the OtaKit plugin\n\n```txt\nnpm install @otakit/capacitor-updater\nnpx cap sync\n```\n\n### 5. Create an OtaKit app and log in\n\nCreate an app in the OtaKit dashboard and copy its `appId`. Then install the CLI and log in:\n\n```txt\nnpm install -g @otakit/cli\notakit login\n```\n\n### 6. Configure the plugin\n\n```txt\nconst config: CapacitorConfig = {\n  appId: "com.example.myapp",\n  appName: "my-app",\n  webDir: "dist",\n  plugins: {\n    OtaKit: {\n      appId: "YOUR_OTAKIT_APP_ID",\n    }\n  }\n};\n```\n\n### 7. Add notifyAppReady()\n\nCall `notifyAppReady()` once your app has rendered on a native device:\n\n```txt\n// src/AppReady.tsx\nimport { useEffect } from "react";\nimport { Capacitor } from "@capacitor/core";\nimport { OtaKit } from "@otakit/capacitor-updater";\n\nexport function AppReady() {\n  useEffect(() => {\n    if (Capacitor.isNativePlatform()) {\n      OtaKit.notifyAppReady();\n    }\n  }, []);\n\n  return null;\n}\n```\n\nRender it near the top of your app:\n\n```txt\n// src/App.tsx\nimport { AppReady } from "./AppReady";\n\nexport default function App() {\n  return (\n    <>\n      <AppReady />\n      <main>...</main>\n    </>\n  );\n}\n```\n\n### 8. Build and run on a device\n\n```txt\nnpm run build\nnpx cap sync\nnpx cap run ios       # or: npx cap run android\n```\n\nNote: The app must be published to the App Store (and/or Play Store) at least once with the OtaKit plugin configured before end users&rsquo; devices can receive live updates.\n\n### 9. Ship your first OTA update\n\nMake a visible change, rebuild, and release:\n\n```txt\nnpm run build\notakit upload --release\n```\n\nRelaunch the app on your device. By default, OtaKit downloads the update in the background and activates it on the next cold launch.\n\n### Keep delta updates small\n\nWith `updateStrategy: \'deltas\'`, OtaKit only downloads files whose contents changed since the device&rsquo;s last bundle — images, fonts, and unchanged chunks are reused from the on-device cache. Anything that rewrites files on every build defeats that.\n\nThe common culprit is a non-deterministic build ID. Next.js (static export) stamps a fresh random ID into every HTML file, RSC payload, and `_next/static/<id>/` path on each build, so a one-line change re-downloads ~20 files instead of one. Pin it so unchanged output stays byte-identical:\n\n```txt\n// next.config.ts\nconst nextConfig = {\n  output: \'export\',\n  // Stable build ID → minimal OTA deltas. Per-chunk content hashes still\n  // handle cache-busting, so nothing goes stale.\n  generateBuildId: async () => \'my-app\',\n};\nexport default nextConfig;\n```\n\nVite already emits stable content-hashed filenames, so no change is needed there. To verify determinism for any bundler, run your production build twice without editing source — the output should be byte-for-byte identical (e.g. compare file hashes). If it is, a no-op release downloads nothing and a one-file change downloads one file.\n\n### Next\n- Read Channels & Runtimes for release tracks and native compatibility boundaries.\n- Use the Plugin API and CLI reference for exact configuration and command details.',
  },
  {
    title: 'Channels & Runtime Version',
    label: 'Channels',
    path: '/docs/channels',
    description:
      'Use channels for rollout tracks and runtimeVersion for native compatibility boundaries.',
    markdown:
      'Channels control which audience receives a release — for example, a `staging` channel for testers and the default channel for everyone else. Runtime version creates a compatibility boundary between native builds and OTA bundles — each side only sees releases meant for its version.\n\nBoth are optional and build-time settings. Start with neither and add them when needed.\n\n### Channels\n\n#### Base channel\n\nIf you omit `channel` from the plugin config, the app uses the unnamed base channel. This is the default and the simplest setup.\n\n```txt\nplugins: {\n  OtaKit: {\n    appId: "YOUR_OTAKIT_APP_ID"\n  }\n}\n```\n\nRelease to the base channel:\n\n```txt\notakit upload --release\n```\n\n#### Named channels\n\nAdd a channel when you want a separate rollout track — for example, internal QA, beta, or a staged production rollout.\n\n```txt\nplugins: {\n  OtaKit: {\n    appId: "YOUR_OTAKIT_APP_ID",\n    channel: "staging"\n  }\n}\n```\n\nRelease to that channel:\n\n```txt\notakit upload --release staging\n```\n\n#### Switching channels at runtime\n\nThe configured channel is only the default — apps can override it at runtime with `setChannel()`. The override persists across launches and takes effect on the next check; pass `null` to go back to the configured channel.\n\n```txt\n// "Join the beta" toggle in a settings screen\nawait OtaKit.setChannel({ channel: \'beta\' });   // next check pulls from beta\nawait OtaKit.setChannel({ channel: null });     // back to the configured channel\n\nconst { channel, source } = await OtaKit.getChannel(); // source: \'override\' | \'config\'\n```\n\nChannel names are public CDN paths — runtime switching is a rollout convenience, not access control.\n\n#### Promoting across channels\n\nYou can upload once, test on one channel, then promote the same bundle to another.\n\n```txt\n# Upload and release to staging\notakit upload --release staging\n\n# Promote that bundle to production later\notakit release <bundle-id> --channel production\n```\n\n### Runtime Version\n\n#### When to use it\n\n`runtimeVersion` is optional. Use it when a new store submission changes what the native shell expects from the web bundle. Devices on the old native build won\'t receive bundles meant for the new one, and devices on the new build won\'t receive old bundles.\n\nWithout `runtimeVersion`, all OTA releases share one lane per channel. With it, each runtime version gets its own lane.\n\n#### How to use it\n\nSet `runtimeVersion` in the plugin config before building.\n\n```txt\nplugins: {\n  OtaKit: {\n    appId: "YOUR_OTAKIT_APP_ID",\n    runtimeVersion: "2026.04"\n  }\n}\n```\n\nWith that config:\n- The plugin only requests releases matching that runtime version.\n- CLI uploads inherit the same runtime version from the config.\n- Old cached OTA bundles from a different runtime are ignored on startup.\n\n### Common setups\n- Single stream — no channel, no runtime version. Everything goes to the base channel.\n- Beta + production — use channels like `beta` and `production` to split audiences.\n- New store baseline: — bump `runtimeVersion` so the new native build starts a fresh OTA lane.',
  },
  {
    title: 'Update Strategies',
    label: 'Update Strategies',
    path: '/docs/update-strategies',
    description:
      'How launchPolicy, resumePolicy, and runtimePolicy combine to control when updates apply.',
    markdown:
      'OtaKit has one set of four policy values and three settings that decide when each one runs. The values — `off`, `shadow`, `apply-staged`, and `immediate` — describe what happens. `launchPolicy`, `resumePolicy`, and `runtimePolicy` describe when it happens. This guide walks through picking a combination; for the full field list see the Plugin API reference.\n\n### The four policy values\n- off — do nothing automatically. Nothing is checked, staged, or applied.\n- shadow — check for the latest update and stage it locally, but never apply it. The current session keeps running on the bundle it already has.\n- apply-staged — apply a bundle if one is already staged. If nothing is staged, it falls back to shadow behavior for that check.\n- immediate — check, stage, and apply the newest bundle right away when one is available. The reload can land mid-session — and since any release can be escalated with `--force-immediate`, you rarely need this as the launch or resume default.\n\n### When each setting runs\n- `launchPolicy` — every normal cold start, once the app\'s runtime lane is already resolved. This is the one most apps tune, since it covers the vast majority of app opens. Default: `apply-staged`.\n- `resumePolicy` — when the app returns to the foreground from the background, throttled by `checkInterval`. Default: `shadow`.\n- `runtimePolicy` — cold start specifically when `runtimeVersion` changes or resolves for the first time. In practice that\'s the app\'s first native install, or a native update where you bumped `runtimeVersion` in the plugin config before that release. Default: `immediate`, so a fresh runtime lane catches up fast instead of waiting on a bundle staged under the old lane.\n\n### Recommended combinations\n\n#### Hosted default — right for most apps\n\n`runtimePolicy: "immediate"`, `launchPolicy: "apply-staged"`, `resumePolicy: "shadow"`. Fast on first install, seamless between launches, and new bundles are ready to go as soon as a session restarts.\n\n#### Update quietly, reload only with consent\n\nSet `launchPolicy` and `resumePolicy` to `shadow`. Bundles stage in the background and nothing ever activates on its own — show a "restart to update" prompt from the `updateStaged` event and call `apply()` on accept (see Events & Listeners ).\n\n#### Full manual control\n\nTurn all three policies `off` and drive the lifecycle yourself with `check()`, `download()`, and `apply()`. Use this when your app owns the entire update experience — see the Manual Flow section of the Plugin API reference.\n\n#### Ship critical fixes instantly\n\nThis one isn\'t a config — it\'s per release. Mark it force immediate in the dashboard\'s release dialog or with `otakit release --force-immediate`: the flag is baked into the signed manifest, and devices on `shadow` or `apply-staged` escalate that one release to immediate — download, apply, and reload on their next check. No rebuild, no config change.\n\nIt\'s "immediate on the next check", not push, and the trial/rollback safety net still applies. Policies set to `off` never fetch a manifest, so they never see the flag. Because the reload can land mid-session, reserve it for broken releases — not routine rollouts.\n\nAlso see Channels & Runtime Version for how rollout audience and native compatibility lanes interact with these policies.',
  },
  {
    title: 'Events & Listeners',
    label: 'Events & Listeners',
    path: '/docs/events',
    description:
      'Subscribe to update lifecycle events — update available, staged, applied, failed, rolled back.',
    markdown:
      "The plugin emits lifecycle events through the standard Capacitor listener API. Events complement — they don't replace — the pull APIs `check()` and `getState()`: listeners tell you the moment something happens while your app is running, and the pull APIs reconcile anything that happened while it wasn't.\n\n```txt\nimport { OtaKit } from '@otakit/capacitor-updater';\n\nOtaKit.addListener('updateStaged', ({ bundle }) => {\n  // show \"Update ready — restart now?\" and call OtaKit.apply() on accept\n});\n```\n\n### Event reference\n- `updateAvailable` (LatestVersion): A check found a newer bundle, before any download starts. Fires on manual check() and at the start of automatic flows.\n- `updateStaged` ({ bundle: BundleInfo }): A bundle was downloaded, verified, and staged, ready to apply. Download and stage are atomic in OtaKit, so this is the single 'downloaded' event.\n- `updateApplied` ({ bundle: BundleInfo }): A newly activated bundle was confirmed healthy — fires when notifyAppReady() confirms the trial bundle, in the reloaded JS context.\n- `downloadFailed` ({ version, runtimeVersion?, releaseId?, channel?, reason }): A download, verification, or extraction failed. Non-terminal: the app keeps running its current bundle and retries on a later cycle.\n- `rollback` ({ version, runtimeVersion?, releaseId?, channel?, reason }): An applied bundle failed its health check (notifyAppReady timeout) and was reverted to the previous bundle.\n\n### Caveats\n- Events fire only while the app process is alive. Nothing is delivered for work that happened while the app was killed or before your listener attached — there is no buffering or replay.\n- Reconcile with `getState()` and `getLastFailure()` on startup. A bundle staged in a previous session shows up in `getState().staged`, and a startup rollback (the app restarted before `notifyAppReady()`) happens before any JS runs, so it can only be observed via `getLastFailure()`.\n- `apply()` destroys the old JS context. A listener registered before the restart will not see `updateApplied` — that event fires in the reloaded bundle. Attach `updateApplied` and `rollback` listeners early in app startup, not inside the restart click handler.\n- One `updateStaged`, no separate \"downloaded\" event. Download, hash verification, and staging are one atomic operation.\n\nThe apply timeline, end to end:\n\n```txt\nupdateStaged            (old context)\n  -> user taps \"Restart\"\n  -> OtaKit.apply()      (old JS context ends here; WebView reloads)\n  -> new bundle boots\n  -> app calls notifyAppReady()\n  -> updateApplied       (NEW context - attach this listener at startup)\n```\n\n### Use cases\n\nFrom the smallest building block to the most hands-off setup — each pattern moves one more step of the flow from your code into the plugin.\n\n#### 1. Check for an update, download if the user says yes\n\nThe simplest pattern, no listeners at all — a \"Check for updates\" button in a settings screen, driven entirely by the pull API:\n\n```txt\nasync function checkForUpdates() {\n  const result = await OtaKit.check();\n  if (result.kind === 'no_update') {\n    toast(\"You're up to date\");\n    return;\n  }\n\n  // update_available or already_staged\n  if (await confirmDownload(result.latest.version)) {\n    const download = await OtaKit.download(); // no-op if already staged\n    if (download.kind === 'staged' && (await confirmRestart())) {\n      await OtaKit.apply(); // terminal: reloads into the new bundle\n    }\n  }\n}\n```\n\n#### 2. Ask before every download\n\nSame consent flow, but event-driven instead of a button — pair manual mode with `updateAvailable` so the user approves the download (useful on metered connections):\n\n```txt\n// capacitor.config.ts: { launchPolicy: 'off', resumePolicy: 'off' }\nOtaKit.addListener('updateAvailable', async (latest) => {\n  if (await confirmDownload(latest.version)) {\n    await OtaKit.download(); // emits updateStaged when done\n  }\n});\nawait OtaKit.check();\n```\n\n#### 3. Auto-download in the background, prompt to apply\n\nThe most hands-off custom flow: `shadow` mode does the checking, downloading, throttling, and deduplication, and your code only owns the restart prompt:\n\n```txt\n// capacitor.config.ts\nplugins: {\n  OtaKit: { launchPolicy: 'shadow', resumePolicy: 'shadow' }\n}\n```\n\n```txt\n// app startup\nconst state = await OtaKit.getState();\nif (state.staged) showRestartPrompt(state.staged);\nOtaKit.addListener('updateStaged', ({ bundle }) => showRestartPrompt(bundle));\n\n// in the prompt's accept handler\nawait OtaKit.apply();\n```\n\n#### 4. Confirm and report after the fact\n\nWorks alongside any of the above (or the fully automatic default) — a toast when an update landed, and error reporting when one failed:\n\n```txt\n// attach at startup, in the reloaded bundle\nOtaKit.addListener('updateApplied', ({ bundle }) => {\n  toast('Updated to ' + bundle.version);\n});\nOtaKit.addListener('downloadFailed', (e) => reportError('ota_download', e));\nOtaKit.addListener('rollback', (e) => reportError('ota_rollback', e));\n\n// startup rollbacks can't reach a listener - reconcile:\nconst failure = await OtaKit.getLastFailure();\nif (failure) reportError('ota_rollback_startup', failure);\n```\n\n### Cleanup\n\n`addListener` resolves to a `PluginListenerHandle`; call `.remove()` on it when the owning UI unmounts, or `OtaKit.removeAllListeners()` to drop everything at once.\n\nAlso see Update Strategies for choosing the policies these events observe, and the Plugin API reference for the full method list.",
  },
  {
    title: 'CI Automation',
    label: 'CI Automation',
    path: '/docs/ci',
    description: 'Use GitHub Actions to build, upload, and optionally release OtaKit bundles.',
    markdown:
      'This workflow builds your app, uploads a bundle, and can either leave it uploaded, release it to the base channel, or release it to a named channel.\n\n### 1. Add repository secrets and variables\n\nGitHub repository secrets:\n\n```txt\nOTAKIT_TOKEN=otakit_sk_...\nOTAKIT_APP_ID=app_...\n```\n\nOptional GitHub repository variables:\n\n```txt\nOTAKIT_RELEASE_CHANNEL=base\n\n# Leave empty or unset for upload-only.\n# Use "base" for the base channel.\n# Use a channel name like "staging" for a named release track.\n```\n\n### 2. Copy this workflow\n\nCreate `.github/workflows/otakit.yml`:\n\n```txt\nname: OTA upload\n\non:\n  push:\n    branches: [main]\n  workflow_dispatch:\n\npermissions:\n  contents: read\n\njobs:\n  upload:\n    runs-on: ubuntu-latest\n    env:\n      OTAKIT_TOKEN: ${{ secrets.OTAKIT_TOKEN }}\n      OTAKIT_APP_ID: ${{ secrets.OTAKIT_APP_ID }}\n      OTAKIT_RELEASE_CHANNEL: ${{ vars.OTAKIT_RELEASE_CHANNEL }}\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 20\n          cache: npm\n\n      - run: npm ci\n      - run: npm run build\n\n      - name: Upload bundle\n        run: |\n          if [ -z "$OTAKIT_RELEASE_CHANNEL" ]; then\n            npx --yes @otakit/cli@latest upload\n          elif [ "$OTAKIT_RELEASE_CHANNEL" = "base" ]; then\n            npx --yes @otakit/cli@latest upload --release\n          else\n            npx --yes @otakit/cli@latest upload --release "$OTAKIT_RELEASE_CHANNEL"\n          fi\n```\n\nThis workflow uses the default bundle path from `capacitor.config.ts`.\n\n### Release channel options\n- Empty or unset `OTAKIT_RELEASE_CHANNEL`: upload only\n- `OTAKIT_RELEASE_CHANNEL=base`: upload and release to the base channel\n- `OTAKIT_RELEASE_CHANNEL=staging`: upload and release to the `staging` channel\n\n### Self-hosted addition\n\nIf you run your own OtaKit server, add this environment variable to the workflow as well:\n\n```txt\nOTAKIT_SERVER_URL: ${{ secrets.OTAKIT_SERVER_URL }}\n```\n\n### Best practices\n- Pin the CLI version after your first successful run instead of using `@latest` forever.\n- Use the base channel for the simplest production setup. Add a named channel only when you need a separate rollout track such as `staging`.\n- Use branch protection so OTA uploads only run from trusted branches.\n\n### Next\n- Use the CLI reference for exact upload, login, and token commands.',
  },
  {
    title: 'AgentKit: MCP & Agent Skills',
    label: 'AgentKit: MCP & Skills',
    path: '/docs/agents',
    description:
      'Connect OtaKit to Codex, Claude Code, VS Code, and other AI agents for project inspection, uploads, releases, monitoring, and reverts.',
    markdown:
      'Give your coding agent the context and tools to ship Capacitor updates without inventing a second release process. AgentKit combines MCP servers for live product access with an open Agent Skill that teaches the agent how to inspect, prepare, publish, monitor, and revert safely.\n\n### Three parts, one release workflow\n- **Local MCP**: Runs through the OtaKit CLI inside your repository, so the agent can inspect native packages, package webDir, and upload the actual build.\n- **Remote MCP**: Connects over HTTPS with scoped OAuth or an organization key for apps, releases, events, account operations, and auditable writes.\n- **Agent Skill**: A portable, vendor-neutral playbook for choosing the right tools, preserving every release option, and asking before consequential writes.\n\nLocal MCP and the Agent Skill work independently of the hosted remote endpoint. Remote MCP is deployment-gated; use local MCP if your OtaKit host has not enabled `/mcp`.\n\n### Quick start with Codex\n\nInstall the open OtaKit Skill once:\n\n```txt\nnpx skills add https://github.com/OtaKit/otakit --skill otakit\n```\n\nSign in to OtaKit, then connect the current Capacitor project:\n\n```txt\nnpx -y @otakit/cli@1.5.0 login\n\ncodex mcp add otakit -- \\\n  npx -y @otakit/cli@1.5.0 mcp --project-root .\n```\n\nRestart or refresh the agent so it discovers the new Skill and MCP tools. Then try:\nInspect this Capacitor project for OtaKit readiness. Check configuration and native compatibility, but do not upload or change anything.\n\n### Connect the remote server\n\nRemote MCP is useful when the client cannot run your local CLI, or when you only need account and release operations. For Codex, request only the scopes you want the connection to expose:\n\n```txt\ncodex mcp add otakit --url https://console.otakit.app/mcp\n\ncodex mcp login \\\n  --oauth-client-registration cimd \\\n  --scopes otakit:read,otakit:app:write,otakit:bundle:write,otakit:release:write,offline_access \\\n  otakit\n```\n\nThe browser flow shows the client, organization, and requested permissions before access is granted. You can revoke the connection from Dashboard → Settings → Connections; revocation invalidates its tokens immediately.\n\nFor non-interactive automation, configure your MCP client to read an organization key from `OTAKIT_TOKEN`. Keep it in the environment or secret manager—never in project JSON. Organization keys retain their existing organization-wide operational authority; OAuth is the fine-grained option for user connections.\n\n### Local or remote?\n- **Inspect the local Capacitor project**: Local MCP — Yes; Remote MCP — No\n- **Validate config and native compatibility**: Local MCP — Yes; Remote MCP — No\n- **Build, package, and upload web assets**: Local MCP — Yes; Remote MCP — No\n- **Read apps, bundles, releases, and events**: Local MCP — Yes; Remote MCP — Yes\n- **Prepare, publish, and revert releases**: Local MCP — Yes; Remote MCP — Yes\n- **Read account status and audit history**: Local MCP — User login; Remote MCP — OAuth user\n\nUse local MCP for repository work and uploads. Use remote MCP for account-only work. You can configure both under different names when one agent needs both contexts.\n\n### Claude Code\n\nAdd the local server to the current project:\n\n```txt\nclaude mcp add --transport stdio --scope project otakit -- \\\n  npx -y @otakit/cli@1.5.0 mcp --project-root .\n```\n\nOr connect an enabled remote deployment and complete browser authorization:\n\n```txt\nclaude mcp add --transport http --scope project \\\n  otakit https://console.otakit.app/mcp\nclaude mcp login otakit\n```\n\nCommit a generated `.mcp.json` only when the whole team should discover the server. Tokens and authorization headers never belong in that file.\n\n### VS Code and GitHub Copilot\n\nCreate `.vscode/mcp.json`, then run MCP: List Servers to review, trust, and start the server:\n\n```txt\n{\n  "servers": {\n    "otakit": {\n      "type": "stdio",\n      "command": "npx",\n      "args": [\n        "-y",\n        "@otakit/cli@1.5.0",\n        "mcp",\n        "--project-root",\n        "${workspaceFolder}"\n      ]\n    }\n  }\n}\n```\n\nFor remote MCP, configure `https://console.otakit.app/mcp` as an HTTP server and use VS Code\'s sign-in flow instead of hardcoding a bearer token.\n\n### Useful workflows\n\n#### Review before upload\n\nPrompt: “Inspect the project, resolve the effective app and lane, and check native compatibility. Show me problems before packaging anything.”\n\n#### Upload without releasing\n\nPrompt: “Build and upload version 2.4.1 for the staging lane. Do not publish it. Return the bundle ID and anything I should review.”\n\n#### Prepare a production release\n\nPrompt: “Prepare this bundle for production with force-immediate off and auto-revert enabled. Show the exact current and proposed state, then wait for approval.”\n\n#### Investigate rollout health\n\nPrompt: “Summarize recent release events and failures for production. Keep client-reported counts separate from unique users or devices.”\n\n#### Revert deliberately\n\nPrompt: “Prepare a revert of production to the previous release. Show the exact target and current lane state; do not execute until I approve.”\n\n### What the safety workflow preserves\n- Exact app, channel, runtime version, bundle, and current release state.\n- Force-immediate, auto-revert thresholds, encryption, compatibility decisions, and every other existing release option.\n- Preview-first publish and revert operations with explicit client approval.\n- Idempotent retries and stale-state rejection instead of duplicate or blind releases.\n- Current organization membership, OAuth scopes, user role, and audit attribution.\n- Honest analytics language: event records are client-reported diagnostics, not unique users, devices, adoption, or proof of cause.\n\nMCP does not replace the OtaKit CLI or API. It exposes the same product behavior through an agent-compatible interface, so the dashboard, CLI, REST API, and agent all agree on what a release means.\n\n### Self-hosting\n\nReplace the hosted origin with your console origin. Remote MCP is `<console-origin>/mcp`. Deploy the agent features disabled, apply the supplied additive migrations through your normal reviewed maintenance process, and enable release reliability, remote MCP, and OAuth separately. OtaKit does not migrate a live database when MCP is enabled.\n\nSee the self-hosting guide for the platform and Agent Skill source for the self-hosted feature flags and rollout checklist.\n\n### Client references\n- Codex MCP documentation\n- Claude Code MCP documentation\n- VS Code MCP documentation\n- Agent Skills specification',
  },
  {
    title: 'Security',
    label: 'Security',
    path: '/docs/security',
    description: 'How OtaKit secures your OTA update delivery pipeline.',
    markdown:
      "OtaKit is designed so that a compromised CDN or network cannot push malicious code to your users. Every layer — from upload to delivery to activation — has a verification step.\n\n### Manifest signing\n\nEvery manifest is signed with ES256 (ECDSA P-256) when a release is published. The plugin verifies the signature before trusting the manifest. A tampered manifest — whether modified in transit, at the CDN edge, or in storage — is rejected.\n\nThe signing key stays on your server. The plugin only holds the public verification key. Self-hosters generate their own key pair with `otakit generate-signing-key`.\n\n### Bundle verification\n\nEvery bundle download is verified against the SHA-256 hash in the signed manifest. If the hash does not match — due to corruption, tampering, or a partial download — the bundle is discarded and the update is not applied.\n\n### End-to-end bundle encryption (optional)\n\nFor code-sensitive or regulated apps, bundles can be encrypted with AES-256-GCM before upload. Generate a key with `otakit generate-encryption-key`, keep it in CI as `OTAKIT_ENCRYPTION_KEY`, and ship the same key in the app's `bundleKeys` config. Object storage and the CDN then only ever hold ciphertext — a leaked URL or bucket exposes nothing readable, and even OtaKit cannot decrypt your code.\n\nThe encryption parameters are covered by the manifest signature, decryption happens on device only after the download passes its hash check, and a decryption failure behaves like a failed download: the running bundle is untouched. This is confidentiality, not DRM — the key ships inside your binary, like every client-side scheme.\n\n### Native compatibility guardrail\n\nThe most common way OTA updates break an app is shipping a web bundle that calls native code the installed shell doesn't have. At upload time the CLI fingerprints your native plugin dependencies and compares them against the channel's current release — new or changed native code produces a warning (or a CI failure with `--fail-on-incompatible`) before the release ever ships.\n\n### Automatic rollback\n\nAfter a new bundle is activated, the app must call `notifyAppReady()` within a configurable timeout (default 10 seconds). If the call does not arrive — because the new bundle crashes, hangs, or breaks — the plugin automatically rolls back to the last known-good bundle.\n\nThis means a bad OTA release self-heals on the device without user intervention.\n\n### Infrastructure\n\nOn the managed OtaKit service, manifests and bundles are served from Cloudflare's global CDN with 300+ edge locations. Download availability inherits Cloudflare's infrastructure SLA. Bundles are stored in Cloudflare R2 with encryption at rest.\n\nThe dashboard and control plane run on isolated infrastructure. Device traffic (manifest fetches, bundle downloads) never touches the dashboard — it goes directly to the CDN.\n\n### Data collection\n\nOtaKit collects only device events (download, applied, rollback, error) for analytics and billing. Events include an app ID, platform, bundle version, and a random event ID. No persistent device identifiers, IP addresses, or personally identifiable information is stored.\n\nSelf-hosters control the full data pipeline. The ingest service and analytics are optional and can be omitted entirely.\n\n### Open source\n\nThe entire OtaKit codebase — plugin, CLI, dashboard, and ingest service — is open source under the MIT license. The signing and verification logic can be audited directly on GitHub .\n\nSelf-hosting gives you full control over infrastructure, keys, and data. No vendor lock-in.\n\n### HTTPS enforcement\n\nThe plugin enforces HTTPS for all manifest and bundle requests. HTTP is only allowed for `localhost` during development when explicitly opted in via `allowInsecureUrls`.\n\n### API authentication\n\nAll dashboard and CLI operations require authentication via organization service keys or session tokens. Service keys currently carry the organization's full operational API authority; they are not fine-grained scopes. Keys are hashed before storage — the raw key is shown once at creation and never stored. Current organization membership permits normal app, upload, and release operations, while owner/admin checks protect team, key, billing, organization settings, and audit administration. Remote OAuth connections additionally intersect their granted scopes with current membership on every call.\n\n### Reporting a vulnerability\n\nIf you discover a security issue, please email security@otakit.app . We aim to respond within 7 business days.",
  },
  {
    title: 'Self-hosting',
    label: 'Self-hosting',
    path: '/docs/self-host',
    description: 'Run the full OtaKit platform on your own infrastructure, step by step.',
    markdown:
      'OtaKit is MIT-licensed and fully open source — everything the hosted service runs is in the public repo. The fastest way to ship OTA updates is the managed service at otakit.app (free tier, zero infrastructure). But if you need full control over delivery, keys, and data, this guide walks you through running your own instance end to end.\n\nThere is no all-in-one Docker image yet. The control plane is a standard Next.js app, so it runs anywhere Node.js runs — a VPS behind nginx, your own container, or Vercel — plus a Postgres database and an S3-compatible bucket. That\'s the whole required stack.\n\n### How it works\n\nThree things to understand before you deploy anything:\nThe console publishes to storage.\nWhen you run\n\notakit upload --release\n, the console API writes the bundle zip and a manifest JSON to your object storage bucket.\nDevices update from the CDN, not from your server.\nThe Capacitor plugin fetches the manifest and bundle straight from the CDN domain in front of the bucket. Your console can be completely down and updates keep working.\nEverything else is optional.\nAnalytics, email, billing, rate limiting, and CDN purge all degrade gracefully when their env vars are unset. You can start with the minimal stack and add pieces later.\n\n### What you need\n\n#### Required\nConsole\n(\n\npackages/console\n) — the Next.js control plane: dashboard, auth, API, and manifest publishing. This is the only server you must run.\nPostgreSQL 14+\n— apps, bundles, releases, users.\nS3-compatible object storage\n— Cloudflare R2 or AWS S3, for bundle zips and manifests.\nA public CDN domain\nin front of the bucket — devices download manifests and bundles from here.\nOne sign-in method\n— Google, Apple, or GitHub OAuth credentials, or email OTP (needs Resend in production).\n\n#### Optional\nIngest Worker + Tinybird\n(\n\npackages/ingest\n,\n\ntinybird/\n) — device event analytics. Without it, updates work fine; the dashboard just shows empty analytics.\nManifest signing\n— ES256 signatures on manifests. Strongly recommended for production; see below.\nResend\n— transactional email (OTP codes, invites). Without it, emails are logged to the server console.\nCloudflare cache purge\n— instant CDN invalidation after a release. Without it, manifests may be stale until the CDN TTL (minutes) expires.\nUpstash Redis\n— API rate limiting. Without it, rate limiting is disabled.\nPolar\n— billing. Leave it unset when self-hosting: all organizations get unlimited usage and the billing UI is hidden.\nPublic site\n(\n\npackages/site\n) — the marketing site and docs you\'re reading now. You don\'t need it.\n\nThe CLI (`packages/cli`) and Capacitor plugin (`packages/capacitor-plugin`) run on your machine and inside your app — you point them at your instance in steps 6 and 7.\n\n### Step 1 — Clone and install\n\nYou need Node.js 20.9+ and pnpm 9+.\n\n```txt\ngit clone https://github.com/OtaKit/otakit\ncd otakit\npnpm install\n```\n\n### Step 2 — Create the database\n\nAny PostgreSQL 14+ works — a managed database (Neon, RDS, Supabase) or your own server. Note the connection string; you\'ll set it as `DATABASE_URL` in step 4.\n\n### Step 3 — Create the storage bucket and CDN\n\nCreate a bucket on Cloudflare R2 or AWS S3 and put a public CDN domain in front of it (e.g. R2 custom domain, or CloudFront for S3). Generate S3 API credentials with read/write access to the bucket. The public domain becomes `CDN_BASE_URL` — devices will fetch manifests and bundles from it, so it must be publicly readable.\n\n### Step 4 — Configure the console\n\nCopy `packages/console/.env.example` to `.env`. It is annotated with the same required/optional split as this page. The required variables:\n\n```txt\n# Database\nDATABASE_URL=postgresql://user:pass@host:5432/otakit\n\n# API write secret for admin/CLI endpoints\nSECRET_KEY=change-me                     # openssl rand -hex 32\n\n# Auth + public URLs\nBETTER_AUTH_SECRET=change-me             # openssl rand -hex 32\nBETTER_AUTH_URL=https://console.your-domain.com\nNEXT_PUBLIC_APP_URL=https://console.your-domain.com\nNEXT_PUBLIC_SITE_URL=https://console.your-domain.com\n\n# At least one sign-in provider\nGOOGLE_CLIENT_ID=...\nGOOGLE_CLIENT_SECRET=...\n# or APPLE_CLIENT_ID / APPLE_CLIENT_SECRET\n# or GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET\n\n# Object storage (R2 shown; any S3-compatible API works)\nR2_BUCKET=otakit-bundles\nR2_ACCESS_KEY=...\nR2_SECRET_KEY=...\nR2_ENDPOINT=https://<account>.r2.cloudflarestorage.com\n\n# Public CDN domain in front of the bucket\nCDN_BASE_URL=https://cdn.your-domain.com\n```\n\nEverything else in `.env.example` (Tinybird, Resend, Polar, Cloudflare purge, Upstash, signing) is optional and covered at the end of this page.\n\n### Step 5 — Migrate, build, start\n\n```txt\ncd packages/console\nnpx prisma migrate deploy\npnpm build\npnpm start\n```\n\nThe console listens on port 3000. Put a reverse proxy (nginx, Caddy) in front with HTTPS — or deploy it like any other Next.js app, to Vercel or your own container platform.\n\n### Step 6 — Sign in and create your app\n\nOpen your console URL, sign in, and create an app. Copy its `appId` and create an API token (`otakit_sk_...`) for the CLI.\n\n### Step 7 — Point the CLI at your instance\n\n```txt\nexport OTAKIT_SERVER_URL=https://console.your-domain.com\nexport OTAKIT_TOKEN=otakit_sk_...\n```\n\nAlternatively set `serverUrl` in the plugin config (next step) — the CLI reads it from `capacitor.config.*` automatically — and authenticate with `otakit login` instead of an env token.\n\n### Step 8 — Configure the plugin\n\nIn your app\'s `capacitor.config.ts`:\n\n```txt\nplugins: {\n  OtaKit: {\n    appId: "YOUR_APP_ID",\n    cdnUrl: "https://cdn.your-domain.com",          // required: your CDN domain\n    serverUrl: "https://console.your-domain.com",   // optional: lets the CLI find your console\n    ingestUrl: "https://ingest.your-domain.com/v1", // optional: only with analytics (below)\n    manifestKeys: [                                  // optional: only with signing (below)\n      { kid: "key-2026-01", key: "MFkwEwYH..." }\n    ]\n  }\n}\n```\n\n### Step 9 — Ship a release and verify\n\n```txt\n# build your web assets, then:\notakit upload --release\n```\n\nConfirm the release appears in your dashboard, and that the manifest is publicly readable at `{CDN_BASE_URL}/manifests/{appId}/__base__/__default__/manifest.json`. Then launch your app: on start or resume the plugin fetches that manifest, downloads the new bundle, and applies it. Your self-hosted pipeline is live.\n\n### Optional: manifest signing (recommended)\n\nSigning lets devices verify manifests were produced by your server, not just served from your CDN. Generate a key pair:\n\n```txt\notakit generate-signing-key\n```\n\nPut the private key in the console env (`MANIFEST_SIGNING_KID`, `MANIFEST_SIGNING_KEY`) and the public key in the plugin config\'s `manifestKeys`. To run without signing, set `MANIFEST_SIGNING_DISABLED=true`.\n\n### Optional: analytics (Ingest Worker + Tinybird)\n\nDevice events (downloaded, applied, rolled back) flow from the plugin to a Cloudflare Worker, which batches them into Tinybird. The dashboard reads its charts and download counts from Tinybird pipes.\n\n```txt\n# 1. Deploy the Tinybird project (datasources + pipes)\ncd tinybird\ntb login\ntb deploy\n\n# 2. Deploy the Worker\ncd packages/ingest\nnpx wrangler secret put TINYBIRD_EVENTS_TOKEN   # Tinybird append token\nnpx wrangler deploy\n```\n\nSee `packages/ingest/README.md` for the Worker\'s queue and rate-limit bindings in `wrangler.jsonc`. Then set `TINYBIRD_API_HOST` and `TINYBIRD_READ_TOKEN` in the console env, and `ingestUrl` in the plugin config.\n\n### Optional: everything else\nEmail\n— set\n\nRESEND_API_KEY\nand\n\nEMAIL_FROM\nto send OTP codes and invites via Resend.\nAuto-revert\n— releases published with the auto-revert flag are reverted automatically when too many devices roll back within 24 hours. Requires analytics (above) plus any scheduler calling\n\nPOST /api/cron/auto-revert\nevery ~10 minutes. If\n\nCRON_SECRET\nis set, pass it as a Bearer header or\n\n?secret=\nquery param; if unset, the endpoint is open (the sweep is idempotent and only reverts releases whose own thresholds trip).\nCDN purge\n— set\n\nCF_ZONE_ID\nand\n\nCF_API_TOKEN\n\n(Cloudflare) so releases invalidate cached manifests instantly.\nRate limiting\n— set\n\nUPSTASH_REDIS_REST_URL\nand\n\nUPSTASH_REDIS_REST_TOKEN\nto rate-limit the API.\nBilling\n— Polar integration powers the hosted service\'s plans. Leave the\n\nPOLAR_*\nvars unset for unlimited usage with no billing UI.\n\nStuck on something? Email us or open an issue on GitHub — self-hosting reports help us make this guide better.',
  },
] as const satisfies readonly DocumentationPage[];
