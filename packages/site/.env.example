# Optional — canonical URLs (used for metadata, sitemap, robots, OG images).
# Defaults to the production hosts when unset.
NEXT_PUBLIC_SITE_URL="https://www.otakit.app"
NEXT_PUBLIC_CONSOLE_URL="https://console.otakit.app"

# Optional — contact form email
RESEND_API_KEY=""
EMAIL_FROM="OtaKit <noreply@otakit.app>"
SUPPORT_EMAIL="support@otakit.app"

# Optional — Google tag (GA4 + Google Ads). Tag renders only when set.
NEXT_PUBLIC_GA_ID=""
NEXT_PUBLIC_GOOGLE_ADS_ID=""
